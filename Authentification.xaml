<Window x:Class="PW7Cl.Authentification"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PW7Cl"
        mc:Ignorable="d"
        Title="Authentification" Height="450" Width="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <StackPanel x:Name="LoginPanel" VerticalAlignment="Center" HorizontalAlignment="Center">
            <TextBlock Text="Введите логин:" Margin="5"/>
            <TextBox x:Name="LoginTextBox" Width="200" Margin="5"/>
            <TextBlock Text="Введите IP сервера:" Margin="5"/>
            <TextBox x:Name="IPTextBox" Width="200" Margin="5"/>
            <TextBlock Text="Введите имя пользователя:" Margin="5"/>
            <TextBox x:Name="NicknameTextBox" Width="200" Margin="5"></TextBox>
            <Button Content="Присоединиться" Click="ConnectButton_Click" Margin="5"/>
        </StackPanel>
    </Grid>
</Window>
